const asymmetricEncryptString = ( encryptConfigObj, textToEncrypt ) => {
    try {


    } catch ( error ) {
        return null;
    }
};

const asymmetricDecryptString = ( decryptConfigObj, textToDecrypt ) => {
    try {


    } catch ( error ) {
        return null;
    }
};

const symmetricEncryptString = ( encryptConfigObj, textToEncrypt ) => {
    try {


    } catch ( error ) {
        return null;
    }
};

const symmetricDecryptString = ( decryptConfigObj, textToDecrypt ) => {
    try {


    } catch ( error ) {
        return null;
    }
};


module.exports = {
    asymmetricEncryptString,
    asymmetricDecryptString,
    symmetricEncryptString,
    symmetricDecryptString
}
